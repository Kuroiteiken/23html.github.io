# Agent instructions

The canonical instructions live in [`docs/AGENTS.md`](docs/AGENTS.md), with a
Turkish translation in [`docs/AGENTS.TR.md`](docs/AGENTS.TR.md). Read that file
before changing anything in this repository.

This pointer exists only so agent tooling that expects `AGENTS.md` at the
repository root still resolves. Do not add instructions here.
